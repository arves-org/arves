\* TLC model-checking configuration for ARVES_Kernel.tla
\* Verification evidence for Global Readiness R-05 / P05. NOT normative spec.
\*
\* Two distinct content tokens (ContentId addresses) is enough to exercise the
\* first commit, the idempotent re-commit, interleaved proposals, and the
\* out-of-order commit paths. The reachable state space is tiny; TLC exhausts it
\* in well under a second (no symmetry or view reduction needed).
\*
\* c1, c2 are TLC model values standing for the concrete ACS-001 ContentIds
\* pinned in ARVES_Kernel_Formal_Spec.md, CS-1:
\*   c1 = 12206623c3d81c6f9a6ecf04ee9d474ffbcb31e29bb45ce01070d6bef20506d63f10
\*   c2 = 1220095e3f1504ab8cee6c3b52ad1344d46e21d3ae4cf527cf119308034bf4345a34

SPECIFICATION Spec

CONSTANTS
    Content = {c1, c2}

\* NO SYMMETRY (fixed 2026-07-05, first captured TLC run — see TLC_RUN.md): this
\* file previously declared `SYMMETRY Perms` while also checking the liveness
\* PROPERTY below. TLC itself warns that symmetry during liveness checking is
\* unsound, and the combination produced a SPURIOUS EventuallyCommitted
\* counterexample (a trace where c2 was never proposed — the leads-to holds
\* trivially for it). The state space is tiny (20 distinct states), so symmetry
\* buys nothing here anyway; the full space is checked exhaustively without it.

\* Safety: checked as a state invariant (holds in every reachable state).
INVARIANT SafetyInv

\* Liveness: a proposed content is eventually reflected in truth. Requires the
\* weak-fairness conjunct that Spec carries.
PROPERTY EventuallyCommitted
