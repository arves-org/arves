# RCR-018 — Master Roadmap de-drift (doc-only, like RCR-003)

**Status:** Applied (v1.1) · **Type:** doc-only corrections inside frozen `runtime/docs/` —
NO logic, NO code, NO public API · **Gate:** `cargo test --workspace` stays green —
**98 passed / 0 failed** verified after the change (doc-only; the workspace does not compile
these files). Freeze re-baseline (`freeze_check.py update`) is deliberately LEFT TO THE
MAINTAINER integration loop per this lane's mandate (no freeze update from this change-set).

## Motivation

`runtime/docs/ARVES_Master_Roadmap.md` is under the frozen `runtime/` root but had drifted from
the living record on two axes, which would mislead a reader doing traceability against the
frozen mirror (the exact failure mode RCR-003 fixed for crate citations):

1. **Stale Kit version:** it cited `arves-standard-kit 0.2.0`; the current Kit is **0.3.1**
   (`standard/VERSION`, advanced by CCP-006/007). Tracked as §F "MR-drift" in
   `verification/OPEN_DEBT_REGISTER.md`.
2. **Stale era/gating claims invalidated by maintainer Ruling 002** (2026-07-05,
   `docs/MAINTAINER_RULINGS.md`): the roadmap still said Industrialization is
   "gated behind Era 3" / "post-G2", that the next milestone is "G2, not I2", and that the
   Product era is "forbidden until the platform is certified" (the last already contradicted
   the doc's own two-arm/IDR-006 section).

## Change (every stale claim fixed, `runtime/docs/ARVES_Master_Roadmap.md`)

| # | Location | Was | Now |
|---|----------|-----|-----|
| 1 | Five-eras table, Era 4 | "⬜ gated behind Era 3" | 🟢 UN-GATED — build proceeding (superseded by Ruling 002; RCR + `docs/design/` packages) |
| 2 | Five-eras table, Era 5 | "⬜ separate org; forbidden until the platform is certified" | 🟢 development OPEN (two-arm pivot + IDR-006); GA still behind the four-condition gate — prior wording struck in place (`~~…~~` + supersession pointer), matching the marker style of rows 1/3/7 |
| 3 | Five-eras table, Era 3 | "🟢 CURRENT ERA" | "🟢 CURRENT — continues in parallel" (Ruling 002 runs Era 4 alongside; G2 stays the honest external exit event) |
| 4 | Two-arm ASCII diagram | "Program 3 INDUSTRIALIZATION ⬜ post-G2" | "🟢 un-gated (Ruling 002 — was 'post-G2')" |
| 5 | STANDARD PROGRAM paragraph | "Program 3 (Industrialization, post-G2)" | "un-gated by Ruling 002; formerly 'post-G2'"; G2 criterion retained, no longer what the build waits on |
| 6 | IDR-006 governance line | `arves-standard-kit 0.2.0` | `arves-standard-kit` current version via `standard/VERSION` (**0.3.1** as of CCP-006/007) — closes §F MR-drift |
| 7 | Program 2 section | "The next milestone is G2 …, not I2." | superseded-in-part blockquote prepended (I2..I6 proceeds in parallel; the G2 program continues unchanged) |

**`runtime/README.md` was audited in the same pass and found CLEAN** — no version, count, or
era claim in it is stale (its "I1 runtime core IMPLEMENTED and runnable" status remains
accurate) — so it is intentionally untouched.

## Scope (honest — what this is and is NOT)

- **IS:** alignment of one frozen `runtime/docs/` mirror to the already-recorded living facts
  (`standard/VERSION` 0.3.1; Ruling 002). Every superseded sentence is *marked* superseded with
  a pointer to `docs/MAINTAINER_RULINGS.md`, never silently deleted, preserving the historical
  record.
- **IS NOT:** a behavior/ABI/spec change. No crate, no `standard/`, no `spec-markdown/`, no
  `corpus/` byte touched. Not a grade change: **independence stays G1**; G2 remains the only
  external exit event and is unaffected.

## Verification

- `cargo test --manifest-path runtime/Cargo.toml --workspace` → **98 passed / 0 failed**
  (unchanged from the RCR-013 baseline — doc-only).
- Companion register updates in living roots: `verification/OPEN_DEBT_REGISTER.md` §F MR-drift
  → CLOSED (this RCR); §E #11 reworded to the precise RCR-012 split; §F V1/V2/V3 annotated with
  the ccp-lane regen-staging pointer. Freeze re-baseline deferred to the maintainer loop.
