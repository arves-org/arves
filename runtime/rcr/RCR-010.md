# RCR-010 — Live L1 conformance: read-only Query node completes the Information→Kernel→Query pipeline (Runtime v1.1)

**Status:** Applied (v1.1) · **Type:** additive (extends `arves-conformance::live`; no existing
behavior/ABI change) · **Gate:** `cargo test --workspace` → **87 passed / 0 failed** (was 85; +2
tests). Freeze baseline re-advanced via `freeze_check.py update`.

## Motivation (strategic-program rank 8 + rank 13)

RCR-008/009 made the Kernel + Information Platform nodes live. The L1 pipeline's last node — the
**Query** — was missing (`grep impl Query` = 0), so there was no end-to-end scenario that reads a
committed truth *back*. This adds the read node and the first **three-node end-to-end** artifact.

## Change (what shipped)

- **`QueryProjection`** (in `arves-conformance::live`): a **read-only** projection over committed
  truth, built by **replaying the persistence WAL** (`WalStore::shards`/`open` → `Wal::replay_from`
  → `ReplayCursor::next`). It is a **customer of the frozen persistence API** and **never touches
  the Kernel** — reads are deliberately not on the `Kernel` trait (ORCH-001/OWN-001, "a gateway,
  not a database"); the layered-correct read path is persistence-side. `query(tenant, workspace)`
  is tenant-scoped; the projection holds no commit path.
- **`QueryProbe`** (impls the frozen `NodeProbe`): commits two tenants' truths via the Kernel, then
  proves the WAL-replay projection is (a) complete (reconstructs both truths) and (b)
  **tenant-isolated** (a query for one tenant never returns the other's payload) → SHARD-001 +
  `TenantWorkspaceIsolation`, derived from behaviour.
- **`l1_full_scenario()` + `run_l1_full_scenario()`**: the first **end-to-end** live artifact —
  a Source is **canonicalized** (Information) → **committed** (Kernel) → **read back** (Query),
  three nodes judged by the `LiveVerdictEngine` → `Verdict::Pass`. The `conformance_live` bin now
  runs it; the evidence probe row → **3-node pipeline, 7 invariant-checks Held** (10/10, drift-proof).
- Tests: `live_l1_full_scenario_passes` + `query_projection_is_read_only_and_tenant_isolated`.

## Scope (honest)

- **IS:** the full L1 **Information→Kernel→Query** pipeline is now a live, drift-proof
  `ConformanceArtifact` — the single L0 behaviour-conformance row is now an **executing, end-to-end
  L1 artifact** emitted from the real codec + Kernel + WAL. Evidence-Level raise (behaviour L0 → L1
  for the core pipeline) at grade **G0/G1**, NOT a G2 event.
- **IS NOT:** the deeper L1–L4 axes (Control Plane / Engine / Capability / multi-agent /
  distributed) — those remain typed-only, gated behind Standard Validation (I2–I6). Only the
  Information/Kernel/Query core triple is populated. The Connector + Query are *verification-side
  reference* nodes in `arves-conformance`; promoting them into the `arves-information-platform` /
  `arves-query` crates is rank 19.

## Verification

- `cargo run -p arves-conformance --bin conformance_live` → 3-node artifact, VERDICT **Pass**
  (deterministic across runs); `cargo test --workspace` → **87/0**; evidence_probe `--check` green.
- `freeze_check.py check` reported drift on **exactly** the sanctioned files (`live.rs`,
  `bin/conformance_live.rs`, this record, RUNTIME_FREEZE); baseline re-advanced. No `standard/` byte
  touched; LAYER-001 gate green (conformance → persistence is a downward edge).
